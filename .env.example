# Local Docker Compose defaults are already defined in docker-compose.yml.
# These values document the runtime configuration expected by the services.

MONGO_URI=mongodb://mongodb:27017/ecommerce
REDIS_URL=redis://redis:6379
KAFKA_BROKER=kafka:9092
CACHE_TTL_SECONDS=60
